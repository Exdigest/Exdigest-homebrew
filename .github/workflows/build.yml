name: Wii Build

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: ghcr.io/devkitpro/devkitppc:latest

    steps:
      - uses: actions/checkout@v4

      - name: Check SDK
        run: |
          echo $DEVKITPRO
          ls $DEVKITPRO
          ls $DEVKITPRO/libogc || true

      - name: Build
        run: |
          make

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: wii-build
          path: |
            **/*.dol
            **/*.elf
