name: Wii Build

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: devkitpro/devkitppc:latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Wii libraries
        run: |
          sudo dkp-pacman -Sy --noconfirm
          sudo dkp-pacman -S --needed --noconfirm libogc

      - name: Build
        run: |
          make

      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: wii-build
          path: |
            *.dol
            *.elf
